# Milestone M3 gate review — chưa qua

Milestone: M3 — Content engine và learning quality  
Build/commit: `8c774b0`  
Reviewer: Codex technical self-audit  
Date: 2026-09-18

## Evidence

| Gate | Result | Evidence | Notes |
|---|---|---|---|
| Hai topic đại diện mỗi lớp | PASS ở mức draft | `src/data/seed.ts`, `src/data/examSample.test.ts` | Lớp 6/7/8/9 có 3/2/2/2 bài; chẩn đoán 8/10/12/14 câu |
| Skill graph, schema, version và exam compatibility | PASS kỹ thuật | `src/data/contentValidation.ts`, `src/data/catalogue.ts`, `src/data/examSample.ts`, 40 unit tests | Mẫu thi 3 câu là bài luyện gốc, không gắn tỉnh/năm chính thức |
| Mỗi bài có objective và exit check | PASS kỹ thuật | `src/data/seed.ts`, `src/data/seed.test.ts` | Câu kiểm tra cuối bài có ID khác câu chẩn đoán |
| MCQ không trùng lựa chọn, đáp án đúng duy nhất về nghĩa | PARTIAL | Validator + unit tests; [hai phiếu do owner cung cấp](../content/M3-REVIEW-RESULTS.md) | Người duyệt báo không có đáp án khác hợp lý cho mẫu 7 câu; một ID trên phiếu owner cần đối chiếu |
| Published item có nguồn, giải thích, tag, revision, reviewer | NOT READY | Toàn bộ item vẫn `draft` | Không dùng tập published rỗng để tuyên bố gate pass |
| Hai người duyệt độc lập mẫu lớp 9/thi | PARTIAL — hai phiếu đã cung cấp | [Kết quả duyệt](../content/M3-REVIEW-RESULTS.md) | Sáu ID khớp cả hai phiếu; `exit-conditional-01` không tồn tại và ngày phiếu owner cần xác nhận trước khi ghi review metadata |
| Learner comprehension sample | FAIL — chưa thực hiện | [Kịch bản kiểm tra tay](M3-MANUAL-TEST.md) | Cần quan sát học sinh hiểu mục tiêu, lời giải và báo lỗi |
| Báo lỗi nội dung | PASS local / chưa gửi | Web E2E smoke; `src/domain/contentReports.ts` | Chẩn đoán, bài học, phát âm ghi ID/version/lý do trên máy; upload/triage thuộc M4 |
| Typecheck, lint, unit, web E2E, Android bundle | PASS | Lệnh trong worklog 2026-09-18 | 40 tests; E2E bao gồm reload và câu đọc dài |
| Owner manual review | PARTIAL — không thấy lỗi trong phần đã thử | [M3 manual test](M3-MANUAL-TEST.md) | Owner đã thử UI local, gửi ảnh Tiến độ lớp 8; chưa ghi kết quả từng bước của kịch bản |
| Android runtime/TalkBack | N/A cho M3 content gate; hoãn kiểm tra | `adb`, `emulator`, `maestro` không có trên PATH; [delivery plan](../DELIVERY_PLAN.md) M2/M7 | M3 không thêm hành vi native; Android bundle pass. Runtime/TalkBack vẫn bắt buộc trước APK M7, hoặc sớm hơn khi có SDK/thiết bị |

## Open issues

- P0/P1: chưa phát hiện trong kiểm tra tự động; chưa thể kết luận cho thiết bị thật.
- P2: Android runtime và TalkBack chưa được kiểm tra trên máy này; theo plan hiện hành, đây là việc hoãn đến trước M7, không phải blocker riêng của M3.
- Gate blockers: đối chiếu ID/ngày trên phiếu review, áp dụng metadata sau duyệt, learner comprehension và owner checklist chi tiết còn thiếu.
- UI self-audit: màn Tiến độ ban đầu dày thông số, nút học tiếp nằm sau danh sách dài, highlight hàng đầu có thể khác bài được đề xuất. Đã đưa bài học tiếp lên trước, rút gọn danh sách, giải thích khi học kiến thức nền và sửa nhãn phát âm mô phỏng; cần owner nhìn lại bản sửa. Chữ phụ/font scale lớn trên Android vẫn chưa được kiểm chứng.
- Known limitations: toàn bộ nội dung là draft; phát âm mô phỏng; mẫu thi M3 chưa có màn làm đề (M6); báo lỗi chưa gửi về đội nội dung (M4).

## Decision

Decision: **FAIL — M3 chưa qua gate**  
Next milestone unlocked: **NO**  
Reason: phần kỹ thuật và dữ liệu mẫu đã kiểm thử, owner đã báo không thấy lỗi, hai phiếu duyệt đã được cung cấp. Tuy nhiên review record còn sai ID/ngày, chưa có quan sát learner comprehension và bản UI vừa sửa chưa được owner xác nhận. Android runtime được hoãn theo quy định M2/M7, không tính là lý do fail M3. Không chuyển sang M4 theo [delivery plan](../DELIVERY_PLAN.md) trước khi các bằng chứng M3 còn thiếu được xử lý.
