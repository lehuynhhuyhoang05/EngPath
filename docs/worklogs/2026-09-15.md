# EngPath worklog — 2026-09-15

Milestone: M0 — Product evidence và nền repository  
Outcome: M0 passed; M1 UX direction unlocked. Learner sessions moved to the M1 evidence gate by owner decision.

## Completed

- Added Expo flat-config ESLint and a zero-warning lint command.
- Added Node `>=22.13` engine requirement.
- Added deterministic Android and web bundle smoke commands.
- Added a GitHub Actions mobile quality workflow using locked dependencies.
- Added content review metadata to the content contract in TypeScript.
- Added reusable content validation for identifiers, versions, skill references, grade fit, sources, review evidence, options and pronunciation references.
- Expanded automated tests from 5 to 7.
- Added learner interview guide, anonymized notes template, tracker, assumption traceability and risk register.
- Proposed a safe single-root repository boundary without touching the nested Git metadata.
- Verified the GitHub remote was empty, preserved the legacy mobile history as a verified bundle and metadata backup, initialized root `main`, and configured `origin`.
- Recorded a new M0 gate audit.
- Created root commit `fb7ac14`, pushed `main` to GitHub and observed Mobile quality run #1 pass every step.

## Verification

| Check | Result |
|---|---|
| `npm run lint` | PASS — zero warnings |
| `npm run typecheck` | PASS |
| `npm test` | PASS — 2 files, 7 tests |
| `npm run bundle:android` | PASS |
| `npm run bundle:web` | PASS |
| `npm ci --dry-run` in `apps/mobile` | PASS — lockfile synchronized |
| GitHub Actions Mobile quality #1 | PASS — install, lint, typecheck, tests, Android/web bundles |

## Not completed

- Learner sessions: 0/8–12 completed; moved to M1 before its design can pass.
- M1 wireframes: not started yet.

## Next action

1. Begin M1 sitemap, grayscale wireframes and clickable core-flow prototype.
2. Validate small-screen layout, touch targets, text scaling and state catalogue.
3. Recruit learners when the clickable prototype is ready.
4. Complete 8–12 sessions and synthesize evidence before passing M1.

## M1 candidate update

- Added sitemap, six task flows, component inventory and system-state catalogue.
- Rebuilt the clickable prototype around the Study Coach direction.
- Added four-tab navigation after onboarding and kept profile/settings outside the tab bar.
- Split orchestration, screens, domain presentation helpers and UI primitives out of the former monolithic `App.tsx`.
- Replaced Unicode UI icons with a centralized local SVG set using the Expo SDK 57-compatible package.
- Removed false precision from diagnostic/result presentation; added tested learning bands and evidence confidence.
- Added a full pronunciation UX simulation including sample, countdown, recording/review/result and permission-denied states, with explicit no-audio/no-AI disclosure.
- Added a grade-9-only exam shell and a two-step local reset for repeatable manual testing.
- Excluded generated `dist` bundles from TypeScript inputs so typecheck remains deterministic while exports are rebuilt.
- Prepared `docs/gates/M1-MANUAL-TEST.md`; M1 remains not ready to pass until owner manual review and learner research are complete.

Verification after the M1 implementation:

- `npm run typecheck`: PASS.
- `npm run lint`: PASS, zero warnings.
- `npm test`: PASS, 3 files / 10 tests.
- `npm run bundle:android`: PASS.
- `npm run bundle:web`: PASS.
- `expo-doctor`: PASS, 21/21 checks.
- Core text/background token pairs: PASS WCAG contrast calculation, minimum measured ratio 4.51:1; manual screen audit remains pending.
- Production-dependency audit: no high/critical finding; 10 moderate transitive findings are inside Expo CLI Apple build tooling, and the breaking forced downgrade was intentionally not applied.

## M1 visual iteration 2

- Owner review found the business/functional flow sound but rejected the visual language as too similar to an AI-generated dashboard.
- Kept the accepted information architecture and learning behavior; did not unlock M2 because M1 visual/usability gates remain open.
- Replaced the repeated rounded-card/pastel-pill grammar with a study-notebook/editorial system: neutral paper, dividers, margin rules, deep green and restrained vermilion.
- Reworked the wordmark, onboarding choices, Today mission, learning path, lesson explanation, pronunciation states, progress and profile surfaces using the same rules.
- Reduced default radii from `10/14/18/24` to `4/8/12/16` and reserved pills for compact metadata.
- Measured all core text/background token pairs again; the minimum normal-text contrast is 4.57:1 after darkening the accent color.
- Added `docs/M1_VISUAL_LANGUAGE.md` and recorded owner round-one feedback as `FIX AND RETEST`.

## M1 visual iteration 3 and self-audit

- Audited the owner-supplied Progress screenshot and confirmed the remaining dashboard-template feel.
- Found a learning-integrity defect: one correct diagnostic answer could still render `Khá vững` with a full percentage bar.
- Made learning-band presentation evidence-aware and added regression cases for one-observation extremes.
- Replaced Progress statistic columns and mastery bars with a natural-language summary, attempt evidence marks, numbered rows and a recommended next action.
- Simplified abstract UI copy and removed internal milestone language from student-facing screens.
- Captured and inspected running 420 × 860 views for Today, Lesson, Pronunciation and Progress.
- Recorded the full internal verdict in `docs/gates/M1-SELF-AUDIT.md`.
- M1 implementation is complete; Android font-scale/TalkBack checks and 8–12 real learner sessions remain external evidence requirements.
