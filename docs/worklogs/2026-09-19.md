# 2026-09-19 — M3 review closeout and M4 handoff

- Owner said the two review forms are complete and asked development to continue. The owner form's `exit-conditional-01` was retained in the audit record and mapped to the only matching packet item, `exit-first-conditional-01`, as an explicit editorial inference. The supplied 2026-09-19 date now matches the current date.
- Catalogue revision 2 promotes only the seven reviewed grade-9 questions and two accompanying lessons from `draft` to `reviewed` (version 2). All other content, including the exam template and pronunciation prompt, remains draft; nothing is published.
- M3 learner comprehension with real students is deferred to M7, before private beta, because the app is not yet stable enough for external learners. This updates the delivery plan rather than pretending that a learner observation happened.
- Local verification: typecheck, lint, 40 unit tests, web phone-frame E2E and Android JS bundle passed. E2E now tries IPv4 and IPv6 localhost because Expo on this Windows machine bound to `::1` while Node first tried IPv4.
- M3 gate passes for the prototype content engine only. M4 begins with backend persistence and safe offline sync; it is not complete.
